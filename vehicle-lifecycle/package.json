{
	"name": "vehicle-lifecycle",
	"version": "1.0.0",
	"description": "Example of a Blockchain application using ODM Business Rules in Smart Contracts",
	"main": "index.js",
	"scripts": {
		"test": "echo \"No test specified\"",
		"deploy": "composer archive create --sourceName . --sourceType dir && composer network deploy --archiveFile vehicle-lifecycle*.bna  -p hlfv1 -i admin -s adminpw && composer network list -n vehicle-lifecycle -p hlfv1 -i admin -s adminpw",
		"update": "composer archive create --sourceName . --sourceType dir && composer network update --archiveFile vehicle-lifecycle*.bna  -p hlfv1 -i admin -s adminpw && composer network list -n vehicle-lifecycle -p hlfv1 -i admin -s adminpw",
		"listVehicles": "node cli.js listVehicles",
		"list": "composer network list -n vehicle-lifecycle -p hlfv1 -i admin -s adminpw",
		"makeSuspiciousTransfer1": "node cli.js makeSuspiciousTransfer1",
		"makeSuspiciousTransfer2": "node cli.js makeSuspiciousTransfer2",
		"makeSuspiciousTransfer3": "node cli.js makeSuspiciousTransfer3",
		"clean": "node cli.js clean",
		"setup": "node cli.js setup",
		"demo": "npm run clean && npm run setup && npm run listVehicles && npm run deployRuleapp ../vehicle-lifecycle-decision-service/output/vehicle.jar 1.0 1.0 && npm run makeSuspiciousTransfer1 && npm run makeSuspiciousTransfer2 && npm run makeSuspiciousTransfer3 && npm run listVehicles",
		"deployRuleapp": "node cli.js deployRuleapp"
	},
	"author": "IBM",
	"license": "IBM",
	"dependencies": {
    	"cli-table": "^0.3.1",
    	"composer-admin": "^0.7.3",
    	"composer-client": "^0.7.3",
    	"config": "^1.24.0",
    	"jsonfile": "^2.4.0",
    	"lodash": "^4.17.4",
    	"sprintf-js": "^1.0.3",
    	"winston": "^2.3.0",
    	"yargs": "^6.5.0"
  }
}
